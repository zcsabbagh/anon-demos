import { IpWithCoordinates } from "./proxy.js";
export interface IpStackClient {
    computeGeographicCoords(ipAddress: string): Promise<IpWithCoordinates>;
    computeCurrentGeographicCoords(proxyUrl?: string): Promise<IpWithCoordinates>;
}
